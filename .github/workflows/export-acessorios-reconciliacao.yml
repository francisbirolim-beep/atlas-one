name: Export Accessories Reconciliation

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/export-acessorios-reconciliacao.yml'
      - 'scripts/export-acessorios-atlas-reconciliacao.sql'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    name: Export accessories (read-only)
    runs-on: ubuntu-latest
    environment: production
    env:
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required secrets
        shell: bash
        run: |
          set -euo pipefail
          test -n "${SUPABASE_DB_PASSWORD:-}" || { echo "ERRO: SUPABASE_DB_PASSWORD ausente"; exit 1; }
          test -n "${SUPABASE_PROJECT_ID:-}" || { echo "ERRO: SUPABASE_PROJECT_ID ausente"; exit 1; }
          if ! [[ "$SUPABASE_PROJECT_ID" =~ ^[a-z0-9]{20}$ ]]; then
            echo "ERRO: SUPABASE_PROJECT_ID nao tem o formato esperado."
            exit 1
          fi

      - name: Ensure PostgreSQL client
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v psql >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y postgresql-client
          fi
          psql --version

      - name: Build IPv4 session pooler URL
        shell: bash
        run: |
          set -euo pipefail
          DB_URL=$(python - <<'PY'
          import os
          import urllib.parse
          project = os.environ['SUPABASE_PROJECT_ID']
          password = urllib.parse.quote(os.environ['SUPABASE_DB_PASSWORD'], safe='')
          print(f"postgresql://postgres.{project}:{password}@aws-0-sa-east-1.pooler.supabase.com:5432/postgres?sslmode=require")
          PY
          )
          echo "SUPABASE_DB_URL=$DB_URL" >> "$GITHUB_ENV"

      - name: Export current Atlas accessories
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p reconciliation-output
          psql "$SUPABASE_DB_URL" \
            --set=ON_ERROR_STOP=1 \
            --csv \
            --file=scripts/export-acessorios-atlas-reconciliacao.sql \
            --output=reconciliation-output/acessorios-atlas-atual.csv

          ROWS=$(python - <<'PY'
          import csv
          with open('reconciliation-output/acessorios-atlas-atual.csv', newline='', encoding='utf-8') as f:
              rows = list(csv.reader(f))
          print(max(len(rows) - 1, 0))
          PY
          )
          echo "Linhas exportadas: $ROWS"
          printf '%s\n' "$ROWS" > reconciliation-output/total-linhas.txt
          sha256sum reconciliation-output/acessorios-atlas-atual.csv > reconciliation-output/sha256.txt

      - name: Upload reconciliation export
        uses: actions/upload-artifact@v4
        with:
          name: acessorios-atlas-reconciliacao
          path: reconciliation-output/
          retention-days: 1
          if-no-files-found: error
