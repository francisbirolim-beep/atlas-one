name: Apply Handoff PR152

on:
  push:
    branches:
      - docs/post-pr152-merge

permissions:
  contents: write

jobs:
  patch:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/post-pr152-merge
      - name: Update handoff docs
        run: node scripts/patch-handoff-pr152.mjs
      - name: Commit docs
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/ai-handoff/CURRENT_STATE.md docs/ai-handoff/NEXT_TASK.md docs/ai-handoff/IMPLEMENTATIONS.md
          git commit -m "docs: registrar correção de anexo da PR 152"
          git push origin HEAD:docs/post-pr152-merge
