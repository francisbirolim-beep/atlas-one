'use client'

import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, Search } from 'lucide-react'
import { tokenAtual } from '@/lib/auth'

type Venda={id:string;numero:number;status:string;atendimento_status?:string|null;cliente_nome?:string|null;vendedor_nome:string;subtotal:number;desconto:number;total:number;finalizada_em:string}
type Detalhe={venda:Venda;itens:any[];pagamentos:any[]}
function moeda(n:number){return Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}
function statusAtendimento(s?:string|null){
 if(s==='aguardando_separacao')return ['Aguardando separação','bg-amber-50 text-amber-700 border-amber-200']
 if(s==='parcial')return ['Parcial','bg-sky-50 text-sky-700 border-sky-200']
 if(s==='entregue')return ['Entregue','bg-emerald-50 text-emerald-700 border-emerald-200']
 if(s==='cancelado')return ['Cancelado','bg-red-50 text-red-700 border-red-200']
 return [s||'—','bg-slate-50 text-slate-600 border-slate-200']
}
function statusItem(s?:string|null){
 if(s==='reservado_outra_unidade')return 'Aguardando separação'
 if(s==='separando')return 'Separando'
 if(s==='em_entrega')return 'Em entrega'
 if(s==='entregue')return 'Entregue'
 return s||'—'
}
export default function Historico(){const [q,setQ]=useState(''),[vendas,setVendas]=useState<Venda[]>([]),[detalhe,setDetalhe]=useState<Detalhe|null>(null),[erro,setErro]=useState('')
 async function api(url:string){const t=await tokenAtual();if(!t)throw new Error('Sessão expirada.');return fetch(url,{headers:{Authorization:`Bearer ${t}`}})}
 async function carregar(v=''){try{const r=await api(`/api/balcao/vendas?q=${encodeURIComponent(v)}`);const j=await r.json();if(!r.ok)throw new Error(j.error);setVendas(j.vendas||[])}catch(e){setErro(e instanceof Error?e.message:'Erro ao carregar vendas.')}}
 async function abrir(id:string){try{const r=await api(`/api/balcao/vendas?id=${id}`);const j=await r.json();if(!r.ok)throw new Error(j.error);setDetalhe(j)}catch(e){setErro(e instanceof Error?e.message:'Erro ao carregar venda.')}}useEffect(()=>{carregar()},[]);useEffect(()=>{const h=setTimeout(()=>carregar(q),300);return()=>clearTimeout(h)},[q])
 return <main className="min-h-screen bg-slate-50 p-4 sm:p-6"><div className="mx-auto max-w-7xl space-y-4"><header className="flex items-center gap-3"><Link href="/balcao" className="rounded-lg border bg-white p-2"><ArrowLeft size={18}/></Link><div><p className="text-xs font-semibold uppercase tracking-[.15em] text-slate-400">Venda Balcão</p><h1 className="text-2xl font-bold">Histórico de vendas</h1><p className="text-sm text-slate-500">Consulta financeira e acompanhamento do atendimento da mercadoria.</p></div></header>{erro&&<div className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{erro}</div>}<section className="rounded-2xl border bg-white p-4"><div className="relative"><Search size={16} className="absolute left-3 top-3 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar por número, cliente ou vendedor..." className="w-full rounded-xl border py-2.5 pl-9 pr-3 text-sm"/></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[900px] text-sm"><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr><th className="p-3">Venda</th><th>Data</th><th>Cliente</th><th>Vendedor</th><th>Atendimento</th><th>Desconto</th><th>Total</th><th></th></tr></thead><tbody>{vendas.map(v=>{const st=statusAtendimento(v.atendimento_status);return <tr key={v.id} className="border-t"><td className="p-3 font-bold">#{v.numero}</td><td>{new Date(v.finalizada_em).toLocaleString('pt-BR')}</td><td>{v.cliente_nome||'Cliente balcão'}</td><td>{v.vendedor_nome}</td><td><span className={`rounded-full border px-2 py-1 text-xs font-semibold ${st[1]}`}>{st[0]}</span></td><td>{moeda(v.desconto)}</td><td className="font-bold text-emerald-700">{moeda(v.total)}</td><td><button onClick={()=>abrir(v.id)} className="rounded-lg border px-3 py-1.5 text-xs font-semibold">Detalhes</button></td></tr>})}</tbody></table></div></section>{detalhe&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4" onClick={()=>setDetalhe(null)}><div className="max-h-[85vh] w-full max-w-3xl overflow-auto rounded-2xl bg-white p-5 shadow-xl" onClick={e=>e.stopPropagation()}><div className="flex justify-between gap-3"><div><h2 className="text-xl font-bold">Venda #{detalhe.venda.numero}</h2><p className="text-sm text-slate-500">{detalhe.venda.cliente_nome||'Cliente balcão'} • {new Date(detalhe.venda.finalizada_em).toLocaleString('pt-BR')}</p>{(()=>{const st=statusAtendimento(detalhe.venda.atendimento_status);return <span className={`mt-2 inline-block rounded-full border px-2 py-1 text-xs font-semibold ${st[1]}`}>{st[0]}</span>})()}</div><button onClick={()=>setDetalhe(null)} className="h-fit rounded-lg border px-3 py-1 text-sm">Fechar</button></div><div className="mt-4 space-y-2">{detalhe.itens.map(i=><div key={i.id} className="grid grid-cols-[1fr_auto] rounded-xl bg-slate-50 p-3 text-sm"><div><strong>{i.produto_codigo}</strong> — {i.produto_nome}<div className="text-xs text-slate-400">{i.quantidade} {i.unidade} × {moeda(i.preco_unitario)}</div><div className="mt-1 text-xs font-semibold text-slate-600">Atendimento: {statusItem(i.atendimento_status)}</div>{i.atendimento_observacoes&&<div className="mt-1 whitespace-pre-wrap text-xs text-slate-400">{i.atendimento_observacoes}</div>}</div><strong>{moeda(i.total_item)}</strong></div>)}</div><div className="mt-4 rounded-xl border p-3"><div className="font-semibold">Pagamentos</div>{detalhe.pagamentos.map(p=><div key={p.id} className="mt-2 flex justify-between text-sm"><span className="capitalize">{String(p.forma).replaceAll('_',' ')}{p.parcelas>1?` • ${p.parcelas}x`:''}</span><strong>{moeda(p.valor)}</strong></div>)}</div><div className="mt-4 flex justify-between border-t pt-3 text-lg"><strong>Total</strong><strong className="text-emerald-700">{moeda(detalhe.venda.total)}</strong></div></div></div>}</div></main>}
